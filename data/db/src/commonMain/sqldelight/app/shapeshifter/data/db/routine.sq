-- routine consists a set of exercises
CREATE TABLE IF NOT EXISTS routine (
    `id` INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    `name` TEXT NOT NULL
);

selectAll:
SELECT *
FROM routine;

insert:
INSERT INTO routine (`id`, `name`)
VALUES (
nullif(:id, 0),
:name
);

